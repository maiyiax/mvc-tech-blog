<div class="comments">
    {{#each this}}
    <section class="card">
        <div class="meta">
            {{user.username}} on {{format_date created_at}}
        </div>
        <div class="card-body">
            <div class="text">
                {{comment_text}}
            </div>

        </div>
    </section>
    {{/each}}
</div>